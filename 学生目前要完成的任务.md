# 学生目前要完成的任务

## 1. 下载并烧录虚拟机镜像

把开发板群里发布的最新版虚拟机镜像下载下来，并烧录到开发板中。

## 2. 申请阿里云服务器并配置 WireGuard

申请一个免费阿里云服务器，选择最便宜的套餐即可。每个账号可试用 3 个月。

需要记住自己的服务器公网 IP。使用 MobaXterm 通过 SSH 远程控制自己的服务器，下载 WireGuard，并配置 `wg0.conf` 文件。

### 服务器端 `wg0.conf`

```ini
[Interface]
PrivateKey = qGMe/bu08byvM58yXwQEHN/93jLw8stByvt8ltSH4Ug=
Address = 10.0.0.1/24
ListenPort = 51820

[Peer] # 客户端 A
PublicKey = XPP/M9zzZ9BA2iIuXm22MizqS19J4xk4ynOSr3qfrFg=
AllowedIPs = 10.0.0.2/32

[Peer] # 客户端
PublicKey = ng3P2aaOLV3zDHaz52C4sK9kg3udQD5VoZ8jI5GTt18=
AllowedIPs = 10.0.0.3/32
```

### 服务器终端命令

```bash
# 查看当前状态（应返回 1）
sysctl net.ipv4.ip_forward

# 若为 0，执行以下命令开启
sudo sysctl -w net.ipv4.ip_forward=1
echo "net.ipv4.ip_forward=1" | sudo tee /etc/sysctl.d/99-wg.conf

# 如果你用 iptables（推荐先执行这条）
sudo iptables -A FORWARD -i wg0 -o wg0 -j ACCEPT

# 检查 FORWARD 链默认策略
sudo iptables -L FORWARD -n | grep -i policy

# 如果显示 policy DROP，需额外放行已建立连接
sudo iptables -A FORWARD -m conntrack --ctstate RELATED,ESTABLISHED -j ACCEPT

# Ubuntu / Debian
sudo apt install iptables-persistent -y
sudo netfilter-persistent save

# CentOS / Rocky
sudo yum install iptables-services -y
sudo service iptables save
```

说明：以上命令用于解决“服务器能 ping 通客户端，但客户端之间不能相互 ping 通”的问题。

## 3. 连接开发板并通过 SSH 控制

将开发板连接外设，开机后连接手机热点。本地电脑也连接同一个手机热点。

在开发板终端输入：

```bash
ifconfig
```

查看手机分配给开发板的 IP 地址。然后打开 MobaXterm，通过 SSH 控制开发板。这样后续不需要连接外设，也可以调控开发板。

## 4. 配置开发板 WireGuard

配置开发板的 WireGuard `wg0.conf`。

### 开发板 `wg0.conf`

```ini
[Interface]
Address = 10.0.0.3/32 # 客户端 A 的 VPN 地址
PrivateKey = kM78FcOM+B5v+qlVaJzYez8jPDIE6LSKN4JHQSY4fkU=
DNS = 114.114.114.114
MTU = 1350

[Peer]
PublicKey = IklX0Iy3UGxVVoH+uX8FLyaPWAtkdDQeqHUA8tNSyHo=
Endpoint = 47.97.58.112:51820 # 替换为服务器的公网 IP
AllowedIPs = 10.0.0.1/32,10.0.0.2/32 # 允许访问的 VPN 子网
PersistentKeepalive = 25 # 保持连接
```

注意：其中公网 IP 需要改为自己的服务器公网 IP。

## 5. 修改开发板 `/etc/fstab` 配置

修改自己开发板上的 `/etc/fstab` 配置文件，将其中的 UUID 改为自己 SD 卡的 UUID。

可以在终端输入指令查看自己的 UUID。

可以通过以下命令查看自己的 SD 卡是否已经挂载：

```bash
df -h
```

如果挂载成功，可以看到一个约 60G 的空间。注意开发板本地内存只有 15G，如果磁盘正常，已用空间应约为 7.4G。

如果占用超出预期，需要手动清理磁盘：

```bash
sudo rm -rf /mnt/sdcard/rknn_images_*
```

如果本地磁盘已满，ROS 节点可能无法启动。正常情况下，在开发板终端输入以下命令会出现 6 个节点：

```bash
rosnode list
```

如果节点数量不对，则说明节点启动异常。

## 6. 配置虚拟机 WireGuard

配置虚拟机的 WireGuard `wg0.conf`。

### 虚拟机 `wg0.conf`

```ini
[Interface]
Address = 10.0.0.2/32 # 客户端 A 的 VPN 地址
PrivateKey = mN6vmHXH2+Z12ovgWziHNmzrOahwg4adEa3xZSZ+dHc=
DNS = 114.114.114.114
MTU = 1350

[Peer]
PublicKey = IklX0Iy3UGxVVoH+uX8FLyaPWAtkdDQeqHUA8tNSyHo=
Endpoint = 47.97.58.112:51820 # 替换为服务器的公网 IP
AllowedIPs = 10.0.0.1/32,10.0.0.3/32 # 允许访问的 VPN 子网
PersistentKeepalive = 25 # 保持连接
```

注意：其中公网 IP 需要改为自己的服务器公网 IP。

## 7. 测试三方互 ping

配置完所有 `wg0.conf` 后，进行三方互 ping。

如果三方都能 ping 通，则网络搭建完成。此时可以关闭 Wi-Fi，并插入 4G 卡。

如果三方不能 ping 通，需要检查虚拟机是否真正连上网，可以尝试：

```bash
ping baidu.com
```

## 8. 室内测试

安装好外设，并按照顺序上电。遥控器左上角拨钮用于控制投弹，将泡沫弹安装上。

在虚拟机中远程控制开发板：

```bash
ssh lckfb@10.0.0.3
```

然后输入：

```bash
rosnode list
```

查看是否有 6 个节点。

可以在虚拟机终端输入：

```bash
rqt_image_view
```

获取摄像头信息。

也可以在开发板终端输入：

```bash
roslaunch kadli speech_recognition.launch
```

打开语音识别。
