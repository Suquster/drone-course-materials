# 烧录操作指南（超详细小白版）

> 本指南帮你完成实验 3.2（飞控固件烧录）和实验 3.3（开发板镜像烧录）
> 所有需要的文件已经在仓库的 `资源包(已解压)/` 目录中准备好了

---

## 实验 3.2：Pixhawk 飞控固件烧录

### 你需要准备什么

| 东西 | 说明 | 在哪找 |
|------|------|--------|
| Pixhawk 飞控板 | 实验室提供的那块电路板，无人机的"大脑" | 实验室/老师发 |
| USB 数据线 | **必须是数据线**，不能用只能充电的线！ | 自备 |
| Windows 电脑 | Mac 不行 | 自备 |
| Mission Planner | 飞控调试软件 | 仓库 `资源包(已解压)/PC端软件安装文件/MissionPlanner/MissionPlanner-stable.rar` |
| 飞控参数文件 | `.param` 格式 | 仓库 `资源包(已解压)/嵌入式系统硬件文件/飞控参数/pixhawk飞控参数.param` |
| 航点文件 | `.waypoints` 格式 | 仓库 `资源包(已解压)/嵌入式系统硬件文件/航点文件参数/` 里有3个（图书馆/西操场/逸夫楼） |

### 第一步：安装 Mission Planner

1. 从仓库下载 `MissionPlanner-stable.rar`
2. 用解压软件（WinRAR / 7-Zip）解压
3. 找到 `.msi` 或 `.exe` 安装文件，双击安装
4. 一路点"下一步"，安装完成

### 第二步：用 USB 线连接 Pixhawk

1. 拿出 Pixhawk 飞控板
2. 找到板子上的 **micro USB 接口**
3. 用 USB 数据线连到电脑
4. 等电脑识别（可能会自动装驱动，等一下就好）

### 第三步：打开 Mission Planner 并连接

1. 打开 Mission Planner 软件
2. 看右上角，有两个下拉框：
   - **第一个框**：选择 `COMxx`（xx 是数字，这就是你的飞控端口）
     - 如果不确定是哪个 COM 口，可以拔掉 USB 线看消失了哪个，再插回去
   - **第二个框**：选择 `115200`（波特率）
3. 点击 **"连接"** 按钮
4. 等几秒，如果左上角出现了飞行仪表盘的画面，就说明 **连接成功了！**

> ⚠️ 连接失败怎么办？
> - 检查 USB 线是不是数据线（有的线只能充电）
> - 换一个 USB 口试试
> - 检查 COM 口是否选对

### 第四步：升级固件（烧录）

1. 点击左上方菜单 **"初始设置"**
2. 点击左边列表的 **"安装固件"**
3. 页面上会显示各种飞机图标，选择你的飞机类型（**固定翼**就选飞机图标）
4. 弹窗提示固件版本，点 **"继续"**
5. **等待下载和烧录**（进度条走完）
6. 看到 **"升级完成"** 字样就成功了！

> ⚠️ 烧录过程中**千万不要拔 USB 线**！拔了可能会变砖

### 第五步：加载参数文件

1. 固件烧完后，**断开连接**，再**重新连接**一次
2. 点击菜单 **"配置/调试"**
3. 点击左边的 **"全参数表"**
4. 点击右边的 **"加载"** 按钮
5. 选择参数文件：`pixhawk飞控参数.param`
6. 弹窗点 **"OK"** → **"Yes"**
7. **重要！** 加载完后点 **"写入参数"**（这样重启后参数才不会丢）

### 第六步：加载航点文件

1. 点击菜单 **"飞行计划"**
2. 点击 **"加载航点文件"**
3. 选择一个航点文件（比如 `xicaochang.waypoints`）
4. 地图上会显示航点路线
5. 点击 **"写入航点"**，把路线写入飞控

### ✅ 实验 3.2 完成！

---

## 实验 3.3：RK3566 开发板系统镜像烧录

### 你需要准备什么

| 东西 | 说明 | 在哪找 |
|------|------|--------|
| RK3566 开发板 | 泰山派开发板 | 实验室/老师发 |
| USB 数据线 | Type-C 数据线 | 自备 |
| Windows 电脑 | | 自备 |
| USB 驱动 | DriverAssitant | 仓库 `资源包(已解压)/嵌入式系统硬件文件/泰山派开发板镜像烧录驱动/第7章.../DriverAssitant_v5.1.1/` |
| RKDevTool 烧录工具 | | 仓库 `资源包(已解压)/嵌入式系统硬件文件/泰山派开发板镜像烧录驱动/第7章.../RKDevTool_Release_v2.92/` |
| 系统镜像文件 | RK3566 的系统 | 百度网盘下载: https://pan.baidu.com/s/1Eqk0sajE_qbHZ1F4HrEybg?pwd=femn |

### 第一步：安装 USB 驱动

**必须先装驱动，否则电脑认不出开发板！**

1. 找到 `DriverAssitant_v5.1.1` 文件夹
2. 双击 `DriverInstall.exe`
3. 点击 **"驱动安装"**
4. 等待安装完成，提示成功即可

### 第二步：下载系统镜像

1. 打开百度网盘链接: https://pan.baidu.com/s/1Eqk0sajE_qbHZ1F4HrEybg?pwd=femn
2. 提取码: `femn`
3. 下载 RK3566 镜像的 zip 文件
4. 解压，里面应该有 `.img` 格式的镜像文件

### 第三步：打开 RKDevTool 烧录工具

1. 找到 `RKDevTool_Release_v2.92` 文件夹
2. 双击 `RKDevTool.exe`（不需要安装，直接运行）
3. 软件打开后，你会看到一个烧录界面

### 第四步：让开发板进入下载模式

这是最关键的一步！

1. **不要先插 USB 线**
2. 找到开发板上的两个小按键：
   - **REC 键**（Recovery）
   - **RST 键**（Reset）
3. 操作顺序：
   - 先 **按住 REC 键不放**
   - 然后 **短按一下 RST 键**（按一下就松）
   - 等 2 秒后 **松开 REC 键**
4. 用 USB 线连接开发板到电脑
5. 如果操作正确，RKDevTool 界面底部会显示 **"发现一个LOADER设备"**

> ⚠️ 如果没有识别到设备：
> - 确认 USB 驱动已安装
> - 重新按 REC + RST 的组合
> - 换一条 USB 线/换一个 USB 口

### 第五步：烧录镜像

1. 在 RKDevTool 中，点击对应的位置加载镜像文件
2. 选择你下载解压好的 `.img` 镜像文件
3. 确认设备已识别（显示 LOADER）
4. 点击 **"执行"** 按钮
5. 等待烧录完成（进度条走完）
6. 看到 **"下载完成"** 就成功了！

### ✅ 实验 3.3 完成！

---

## 烧录完成后的下一步

烧录完成后，你需要：

1. **配置开发板 WireGuard VPN**（实验 8）— 我可以帮你做
2. **部署 best.rknn 模型到开发板** — 通过 SSH 上传文件
3. **修改 /etc/fstab 配置** — 挂载 SD 卡存储

这些在你拿到阿里云服务器后，我都可以远程帮你操作。

---

*生成时间: 2026-06-15*
